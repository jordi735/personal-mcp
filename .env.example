MCP_AUTH_TOKEN=
PORT=3000

# GitHub Personal Access Token with "gist" scope (classic) or "Gists: Read and write" (fine-grained).
# Optional — if unset, gist tools are not registered and the server still runs.
GITHUB_PAT=
